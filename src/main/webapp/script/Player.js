this.Player = Class.extend({
init:
    function(control, tileSet, x, y)
    {
        this.control = control;
        this.tileSet = tileSet;
        this.x = x;
        this.y = y;
        this.action = 0;
    },
draw:    
    function(ctx)
    {
        
    }
});